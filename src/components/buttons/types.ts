export interface IButtonProps {
  btnTitle: string;
  genStyles: string;
  uniqueStyles: string;
}

export interface IButtonIcons {
  styles: string;
  handler;
  btnTitle: string;
  disable: boolean;
}
